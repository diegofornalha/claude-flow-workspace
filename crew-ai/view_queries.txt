🔍 QUERIES PARA VISUALIZAR NO NEO4J BROWSER:

1. Ver sistema de aprendizado:
   MATCH (p:Pattern)-[*]-(connected)
   RETURN p, connected

2. Ver memórias sincronizadas:
   MATCH (m:Memory {mcp_synced: true})
   RETURN m ORDER BY m.created_at DESC

3. Ver execuções e telemetria:
   MATCH (e:Execution)-[*]-(connected)
   WHERE e.project = 'Conductor-Baku'
   RETURN e, connected

4. Ver grafo completo de conhecimento:
   MATCH path = (n)-[*..2]-(m)
   WHERE n:Agent OR n:Memory OR n:Pattern
   RETURN path LIMIT 200

5. Ver evolução do aprendizado:
   MATCH (p:Pattern)
   RETURN p.type, p.score, p.occurrences
   ORDER BY p.score DESC

6. Ver Bridge em ação:
   MATCH (m:Memory {mcp_synced: true})-[r]-(connected)
   RETURN m, r, connected
   ORDER BY m.created_at DESC
   LIMIT 50

📊 Abra http://localhost:7474 e execute essas queries!